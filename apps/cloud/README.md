# Firebase Functions (Hono)

This package contains the Firebase Cloud Functions, implemented using Hono.

## Commands

```bash
# Install dependencies
pnpm install

# Build
pnpm build

# Serve locally (firebase emulator)
pnpm serve

# Deploy
pnpm deploy
```
